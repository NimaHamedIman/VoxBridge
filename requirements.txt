# --- Web API (production) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # required by FastAPI for Form/File uploads
slowapi>=0.1.9                 # rate limiting

# --- AI backend ---
groq>=0.9.0

# --- Speech-to-Text ---
openai-whisper>=20231117       # needs ffmpeg installed on the system

# --- Configuration ---
python-dotenv>=1.0.0

# --- Local CLI mode only (src/main.py) ---
sounddevice>=0.4.6
numpy>=1.24.0
pyttsx3>=2.90                  # offline TTS, not used by the web API

# --- Development ---
pytest>=7.0.0
black>=23.0.0
